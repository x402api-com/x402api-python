# coding: utf-8

"""
    x402api Public API

    Public External-Wallet x402 integration endpoints for programmatic charges, readiness, receiving addresses, resources, payments, receipts, and facilitator discovery.

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from x402api.models.fee_policy_mode_input_enum import FeePolicyModeInputEnum
from x402api.models.fee_policy_quote_currency_input_enum import FeePolicyQuoteCurrencyInputEnum
from x402api.models.network_fee_evidence import NetworkFeeEvidence
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class NetworkFeeAlternative(BaseModel):
    """
    NetworkFeeAlternative
    """ # noqa: E501
    type: StrictStr
    version: Annotated[int, Field(strict=True, ge=1)]
    network: Annotated[str, Field(strict=True, max_length=80)]
    asset_id: Annotated[str, Field(strict=True, max_length=80)] = Field(alias="assetId")
    contract_address: Annotated[str, Field(strict=True, max_length=160)] = Field(alias="contractAddress")
    fee_mode: FeePolicyModeInputEnum = Field(alias="feeMode")
    quote_currency: FeePolicyQuoteCurrencyInputEnum = Field(alias="quoteCurrency")
    listed_amount_atomic: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="listedAmountAtomic")
    fee_allowance_cap_quote_micros: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="feeAllowanceCapQuoteMicros")
    estimated_native_fee_atomic: Optional[Annotated[str, Field(strict=True, max_length=78)]] = Field(alias="estimatedNativeFeeAtomic")
    native_symbol: Optional[Annotated[str, Field(strict=True, max_length=16)]] = Field(alias="nativeSymbol")
    native_decimals: Optional[Annotated[int, Field(le=30, strict=True, ge=0)]] = Field(alias="nativeDecimals")
    native_usd_quote_micros: Optional[Annotated[str, Field(strict=True, max_length=78)]] = Field(alias="nativeUsdQuoteMicros")
    estimated_fee_quote_micros: Optional[Annotated[str, Field(strict=True, max_length=78)]] = Field(alias="estimatedFeeQuoteMicros")
    provider_disagreement_bps: Optional[Annotated[int, Field(le=10000, strict=True, ge=0)]] = Field(alias="providerDisagreementBps")
    fee_allowance_quote_micros: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="feeAllowanceQuoteMicros")
    fee_allowance_atomic: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="feeAllowanceAtomic")
    buyer_payment_atomic: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="buyerPaymentAtomic")
    tenant_proceeds_atomic: Annotated[str, Field(strict=True, max_length=78)] = Field(alias="tenantProceedsAtomic")
    quote_expires_at: Optional[datetime] = Field(alias="quoteExpiresAt")
    fee_evidence: NetworkFeeEvidence = Field(alias="feeEvidence")
    fee_evidence_digest: Annotated[str, Field(strict=True, max_length=71)] = Field(alias="feeEvidenceDigest")
    eligible: StrictBool
    exclusion_reason: Optional[Annotated[str, Field(strict=True, max_length=80)]] = Field(alias="exclusionReason")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["type", "version", "network", "assetId", "contractAddress", "feeMode", "quoteCurrency", "listedAmountAtomic", "feeAllowanceCapQuoteMicros", "estimatedNativeFeeAtomic", "nativeSymbol", "nativeDecimals", "nativeUsdQuoteMicros", "estimatedFeeQuoteMicros", "providerDisagreementBps", "feeAllowanceQuoteMicros", "feeAllowanceAtomic", "buyerPaymentAtomic", "tenantProceedsAtomic", "quoteExpiresAt", "feeEvidence", "feeEvidenceDigest", "eligible", "exclusionReason"]

    @field_validator('asset_id', mode="before")
    def asset_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[-a-zA-Z0-9_]+$", value):
            raise ValueError(r"must validate the regular expression /^[-a-zA-Z0-9_]+$/")
        return value

    @field_validator('listed_amount_atomic', mode="before")
    def listed_amount_atomic_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[1-9][0-9]*$", value):
            raise ValueError(r"must validate the regular expression /^[1-9][0-9]*$/")
        return value

    @field_validator('fee_allowance_cap_quote_micros', mode="before")
    def fee_allowance_cap_quote_micros_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('estimated_native_fee_atomic', mode="before")
    def estimated_native_fee_atomic_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('native_usd_quote_micros', mode="before")
    def native_usd_quote_micros_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[1-9][0-9]*$", value):
            raise ValueError(r"must validate the regular expression /^[1-9][0-9]*$/")
        return value

    @field_validator('estimated_fee_quote_micros', mode="before")
    def estimated_fee_quote_micros_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('fee_allowance_quote_micros', mode="before")
    def fee_allowance_quote_micros_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('fee_allowance_atomic', mode="before")
    def fee_allowance_atomic_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('buyer_payment_atomic', mode="before")
    def buyer_payment_atomic_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('tenant_proceeds_atomic', mode="before")
    def tenant_proceeds_atomic_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]*)$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]*)$/")
        return value

    @field_validator('fee_evidence_digest', mode="before")
    def fee_evidence_digest_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^sha256:[0-9a-f]{64}$", value):
            raise ValueError(r"must validate the regular expression /^sha256:[0-9a-f]{64}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NetworkFeeAlternative from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of fee_evidence
        if self.fee_evidence:
            _dict['feeEvidence'] = self.fee_evidence.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if estimated_native_fee_atomic (nullable) is None
        # and model_fields_set contains the field
        if self.estimated_native_fee_atomic is None and "estimated_native_fee_atomic" in self.model_fields_set:
            _dict['estimatedNativeFeeAtomic'] = None

        # set to None if native_symbol (nullable) is None
        # and model_fields_set contains the field
        if self.native_symbol is None and "native_symbol" in self.model_fields_set:
            _dict['nativeSymbol'] = None

        # set to None if native_decimals (nullable) is None
        # and model_fields_set contains the field
        if self.native_decimals is None and "native_decimals" in self.model_fields_set:
            _dict['nativeDecimals'] = None

        # set to None if native_usd_quote_micros (nullable) is None
        # and model_fields_set contains the field
        if self.native_usd_quote_micros is None and "native_usd_quote_micros" in self.model_fields_set:
            _dict['nativeUsdQuoteMicros'] = None

        # set to None if estimated_fee_quote_micros (nullable) is None
        # and model_fields_set contains the field
        if self.estimated_fee_quote_micros is None and "estimated_fee_quote_micros" in self.model_fields_set:
            _dict['estimatedFeeQuoteMicros'] = None

        # set to None if provider_disagreement_bps (nullable) is None
        # and model_fields_set contains the field
        if self.provider_disagreement_bps is None and "provider_disagreement_bps" in self.model_fields_set:
            _dict['providerDisagreementBps'] = None

        # set to None if quote_expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.quote_expires_at is None and "quote_expires_at" in self.model_fields_set:
            _dict['quoteExpiresAt'] = None

        # set to None if exclusion_reason (nullable) is None
        # and model_fields_set contains the field
        if self.exclusion_reason is None and "exclusion_reason" in self.model_fields_set:
            _dict['exclusionReason'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NetworkFeeAlternative from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "version": obj.get("version"),
            "network": obj.get("network"),
            "assetId": obj.get("assetId"),
            "contractAddress": obj.get("contractAddress"),
            "feeMode": obj.get("feeMode"),
            "quoteCurrency": obj.get("quoteCurrency"),
            "listedAmountAtomic": obj.get("listedAmountAtomic"),
            "feeAllowanceCapQuoteMicros": obj.get("feeAllowanceCapQuoteMicros"),
            "estimatedNativeFeeAtomic": obj.get("estimatedNativeFeeAtomic"),
            "nativeSymbol": obj.get("nativeSymbol"),
            "nativeDecimals": obj.get("nativeDecimals"),
            "nativeUsdQuoteMicros": obj.get("nativeUsdQuoteMicros"),
            "estimatedFeeQuoteMicros": obj.get("estimatedFeeQuoteMicros"),
            "providerDisagreementBps": obj.get("providerDisagreementBps"),
            "feeAllowanceQuoteMicros": obj.get("feeAllowanceQuoteMicros"),
            "feeAllowanceAtomic": obj.get("feeAllowanceAtomic"),
            "buyerPaymentAtomic": obj.get("buyerPaymentAtomic"),
            "tenantProceedsAtomic": obj.get("tenantProceedsAtomic"),
            "quoteExpiresAt": obj.get("quoteExpiresAt"),
            "feeEvidence": NetworkFeeEvidence.from_dict(obj["feeEvidence"]) if obj.get("feeEvidence") is not None else None,
            "feeEvidenceDigest": obj.get("feeEvidenceDigest"),
            "eligible": obj.get("eligible"),
            "exclusionReason": obj.get("exclusionReason")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj
